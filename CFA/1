#include<bits/stdc++.h>
using namespace std;
int main(){
    int n;string a;cin>>n>>a;
    stack<char> s;
    for(int i=0;a[i];++i){
        if(a[i]==s.top())s.push(a[i]);
        else s.pop();
    }
    cout<<s.size()<<endl;
}
